# Only needed if you run this app OUTSIDE Streamlit-in-Snowflake (local dev).
# Copy to .streamlit/secrets.toml and fill in your values — never commit the
# real secrets.toml to git (it's already in .gitignore).

[connections.snowflake]
account = "UIGXFIN-WB39887"
user = "kiran"
password = "your_actual_snowflake_password"
role = "ACCOUNTADMIN"
warehouse = "COMPUTE_WH"
database = "INSURANCE_AI_HUB"
schema = "PUBLIC"
